# 案件板内容编辑不应隐式改变样式

- 现象：编辑后选中状态保留，连线变成金色；卡片类型与状态也会改变外观，特定备注会换成另一种封签。
- 原因：内容、选择状态和展示样式耦合，用户无法判断是内容编辑还是主动调色造成变化。
- 修复：统一默认卡片和连线样式；选中仅加外侧虚线提示。外观使用独立可选style字段，只有“调整样式”明确保存才改变。
- 预防：覆盖改名/改备注后样式保持、取消/重置、跨设备恢复及旧客户端省略字段的兼容测试；浏览器检查computed style，避免旧CSS覆盖自定义颜色。

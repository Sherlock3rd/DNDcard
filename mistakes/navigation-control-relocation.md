# 搬移页面控件时保留行为和加载保护

2026-09-16：统一导航后，页面工具不再属于原header或角色容器。只调整CSS不足以保证原有选择器和事件继续工作。

处理：搬移真实DOM节点保留事件；地图上下文使用明确ID，预览页面保留旧选择器兼容；升级按钮离开characterApp后仍纳入档案加载保护。旧header避免留下不可见但可Tab聚焦的入口，手机始终保留四个主导航。

验收包括实际点击、存档不变、当前页标记、路由切换、面板收起和手机滚动；不能只对DOM存在与否进行检查。

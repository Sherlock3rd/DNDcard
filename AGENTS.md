# DNDcard 项目指引

- 本项目继承 `C:\Users\liuweichen\.codex\AGENTS.md` 的全局规则。
- 项目基础规范见 `spec/rules-bootstrap-spec.md`，执行规则见 `rules/rules.md`。
- 每次开始任务前，先检索 `mistakes/` 中与当前工作相关的记录。
- 任何影响 D&D 5e 规则解释、角色卡字段语义、计算公式或玩家数据结构的变更，必须先获得用户确认。
- 环境、脚手架、文档、测试和部署配置可直接处理，但必须写入 `session/session.md`。
